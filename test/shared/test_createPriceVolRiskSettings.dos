// ============================================================================
// test_createPriceVolRiskSettings.dos
// Per-function test suite for caplib::createPriceVolRiskSettings (shared).
// Signature: caplib::createPriceVolRiskSettings(vanna BOOL, priceShift DOUBLE, volShift DOUBLE, method INT, granularity INT, priceScaling DOUBLE, volScaling DOUBLE, threading INT, handle STRING[, returnJson BOOL])
// Uses the native DolphinDB testing framework:
//     test("test/shared")            // whole domain directory
//     python run_tests.py --file shared/test_createPriceVolRiskSettings.dos   // single file
// Generated by tools/gen_function_tests - edit metadata/templates, not this file.
// ============================================================================

// ============================================================================
// Helper predicates
// ============================================================================

def isHandleJson(result, expectedHandle) {
    return strpos(typestr(result), "STRING VECTOR") >= 0 &&
        size(result) == 2 && result[0] == expectedHandle && strlen(result[1]) > 0;
}

def hasJsonField(json, fieldName) {
    return strpos(json, "\"" + fieldName + "\"") >= 0;
}

def isPricingResultsJson(result, expectedHandle) {
    return isHandleJson(result, expectedHandle) &&
        hasJsonField(result[1], "present_value") &&
        hasJsonField(result[1], "cash_value") &&
        hasJsonField(result[1], "currency");
}

def presentValue(pricingResult) {
    json = pricingResult[1];
    marker = "\"present_value\":";
    start = strpos(json, marker);
    if (start < 0) { return double(NULL); }
    start += strlen(marker);
    rest = substr(json, start);
    end = strpos(rest, ",");
    if (end < 0) { end = strpos(rest, "}"); }
    return double(substr(rest, 0, end));
}

def isDoubleVector(value) {
    return strpos(typestr(value), "DOUBLE VECTOR") >= 0;
}

def isMatrix(value) {
    return strpos(typestr(value), "MATRIX") >= 0;
}

// ============================================================================
// Tests
// ============================================================================

@testing:case="createPriceVolRiskSettings valid call"
result = caplib::createPriceVolRiskSettings(1, 0.01, 0.01, 0, 0, 0.01, 0.01, 0, "TF_PRICE_VOL_RISK", false)
assert 1, result == "TF_PRICE_VOL_RISK"

@testing:case="createPriceVolRiskSettings rejects STRING method", exception=1
caplib::createPriceVolRiskSettings(1, 0.01, 0.01, "1", 0, 0.01, 0.01, 0, "TF_PV_RISK_BAD")

// ----------------------------------------------------------------------------
// Negative / exception validation (auto-derived from the signature)
// ----------------------------------------------------------------------------

@testing:case="createPriceVolRiskSettings rejects missing argument", syntaxError=1
caplib::createPriceVolRiskSettings(1, 0.01, 0.01, 0, 0, 0.01, 0.01, 0)

@testing:case="createPriceVolRiskSettings rejects extra argument", syntaxError=1
caplib::createPriceVolRiskSettings(1, 0.01, 0.01, 0, 0, 0.01, 0.01, 0, "TF_PRICE_VOL_RISK", false, 1)

@testing:case="createPriceVolRiskSettings rejects wrong type for priceShift", exception=1
caplib::createPriceVolRiskSettings(1, "bad", 0.01, 0, 0, 0.01, 0.01, 0, "TF_PRICE_VOL_RISK", false)

@testing:case="createPriceVolRiskSettings rejects wrong type for volShift", exception=1
caplib::createPriceVolRiskSettings(1, 0.01, "bad", 0, 0, 0.01, 0.01, 0, "TF_PRICE_VOL_RISK", false)

@testing:case="createPriceVolRiskSettings rejects wrong type for method", exception=1
caplib::createPriceVolRiskSettings(1, 0.01, 0.01, "1", 0, 0.01, 0.01, 0, "TF_PRICE_VOL_RISK", false)

@testing:case="createPriceVolRiskSettings rejects null method", exception=1
caplib::createPriceVolRiskSettings(1, 0.01, 0.01, int(), 0, 0.01, 0.01, 0, "TF_PRICE_VOL_RISK", false)

@testing:case="createPriceVolRiskSettings rejects keyword arguments", syntaxError=1
caplib::createPriceVolRiskSettings(vanna=1, priceShift=0.01, volShift=0.01, method=0, granularity=0, priceScaling=0.01, volScaling=0.01, threading=0, handle="TF_PRICE_VOL_RISK", returnJson=false)
